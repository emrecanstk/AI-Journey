# 21 August 2026

Success bar for tonight: if I see vector, matrix, dot product, softmax, gradient, or loss in a paper, I can tell which part of an AI system it belongs to. Not: I have learned the math completely.

Daily log shape from now on, when possible:

```
CONCEPT
  ↓
WHY
  ↓
MINI EXPERIMENT
  ↓
RESULT
  ↓
WHAT I LEARNED
  ↓
WHAT I DON'T KNOW
  ↓
NEXT
```

## [EN]

### Today's Goal

Understand the mathematical foundations behind AI
without going deeply into the mathematics.

### Learned

- Vector
- Matrix
- Dot Product
- Logits
- Probability
- Softmax
- Loss
- Gradient
- Backpropagation
- Gradient Descent

### AI Connection

Vector/Matrix operations
→ Neural Networks
→ Attention
→ Transformers
→ LLMs

Loss
→ Backpropagation
→ Gradient
→ Parameter Update

### Experiments

- Dot product (`experiments/math-basics/linear_algebra.py`) — `[1,2,3] · [4,5,6] = 32`
- Softmax (`experiments/math-basics/softmax.py`) — raising a logit from 7.2 to 10.0 sharpened `sat`
- Gradient descent toy (`experiments/math-basics/gradient_descent.py`) — weight 2.5 → ~4.96, pred → ~9.86

### Key realization

AI is fundamentally a combination of
mathematical operations, learned parameters,
data and optimization.

Training moves weights. Inference does not.

Backpropagation computes gradients.
Gradient descent updates parameters.

### Questions

- How is backpropagation computed through a full stack of layers, not a toy weight?
- Why does softmax use `exp`, not some other positive function?
- How is learning rate chosen in real LLM training?

### Confidence

7 / 10

Bird's-eye placement of terms: yes.
Derivations and full backprop: no. That is today's intended level.

## [TUR]

### Today's Goal

AI'nin arkasındaki matematiksel temeli, matematiğe derinlemesine girmeden anlamak.

### Learned

- Vector
- Matrix
- Dot Product
- Logits
- Probability
- Softmax
- Loss
- Gradient
- Backpropagation
- Gradient Descent

### AI Connection

Vector/Matrix işlemleri
→ Neural Networks
→ Attention
→ Transformers
→ LLMs

Loss
→ Backpropagation
→ Gradient
→ Parameter Update

### Experiments

- Dot product (`experiments/math-basics/linear_algebra.py`) — `[1,2,3] · [4,5,6] = 32`
- Softmax (`experiments/math-basics/softmax.py`) — logit 7.2 → 10.0, `sat` sivrileşti
- Gradient descent oyuncağı (`experiments/math-basics/gradient_descent.py`) — weight 2.5 → ~4.96, pred → ~9.86

### Key realization

AI temelde matematiksel işlemler, öğrenilmiş parametreler,
veri ve optimizasyonun birleşimidir.

Training ağırlıkları hareket ettirir. Inference ettirmez.

Backpropagation gradient hesaplar.
Gradient descent parametreleri günceller.

### Questions

- Tam bir katman yığınında backpropagation nasıl hesaplanır, oyuncak weight değil?
- Softmax neden başka pozitif fonksiyon değil de `exp` kullanır?
- Gerçek LLM eğitiminde learning rate nasıl seçilir?

### Confidence

7 / 10

Terimleri sistemde yerleştirmek: evet.
Türev ve tam backprop: hayır. Bugünün hedef seviyesi bu.
